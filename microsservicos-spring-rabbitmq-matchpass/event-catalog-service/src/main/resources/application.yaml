server:
  port: 8080
spring:
  application:
    name: event-catalog-service
  profiles:
    active: test
management:
  endpoints:
    web:
      exposure:
        include:
          - '*'
  endpoint:
    health:
      show-details: ALWAYS