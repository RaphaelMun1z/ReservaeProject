server:
  port: 8100

spring:
  application:
    name: inventory-service
  config:
    import: optional:configserver:http://localhost:8888
  cloud:
    config:
      profile: dev
  data:
    redis:
      host: localhost
      port: 6379