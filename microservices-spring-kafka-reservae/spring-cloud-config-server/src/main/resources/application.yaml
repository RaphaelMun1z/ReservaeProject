server:
  port: 8888
spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: ${SPRING_CLOUD_CONFIG_SERVER_GIT_URI}
          search-paths:
            - microservices-spring-kafka-reservae/reservae-config-server
            - microservices-spring-kafka-reservae/reservae-config-server/*
